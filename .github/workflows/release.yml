name: Release to Zenodo
on:
  release:
    types: [created]
jobs:
  zenodo:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Notify
      run: echo 'Release triggered - Zenodo integration active'